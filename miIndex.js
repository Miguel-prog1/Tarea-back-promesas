const { funcionalidadBancaria} = require(`./cuentaBancaria`)
const {funcionalidadAlumnos} = require (`./instituto`)
const {funcionalidadMercado} = require (`./mercadoRestringido`)



/*Ya las funciones contienen ejemplos en sus archivos, si quiere verlos inicie la funciones uno por uno.*/

funcionalidadAlumnos()

funcionalidadBancaria()

funcionalidadMercado()